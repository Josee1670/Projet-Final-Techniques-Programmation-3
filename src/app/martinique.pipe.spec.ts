import { MartiniquePipe } from './martinique.pipe';

describe('MartiniquePipe', () => {
  it('create an instance', () => {
    const pipe = new MartiniquePipe();
    expect(pipe).toBeTruthy();
  });
});
